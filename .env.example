# Server Configuration
HOST=0.0.0.0
PORT=4003

# Database Configuration (single URL)
DATABASE_URL=postgres://taskmanager:password123@localhost:5433/taskmanager?sslmode=disable
DB_PREFER_SIMPLE_PROTOCOL=false

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_ISSUER=iam-authorization-service
JWT_AUDIENCE=platform-console

# Tenant Configuration
DEFAULT_TENANT_ID=default

# Environment
ENV=development

# Optional
CONFIG_PATH=src/config/config.yaml
SEED_ON_STARTUP=false
ALLOW_SEED_ENDPOINT=false
CORS_ALLOWED_ORIGINS=http://localhost:3005,https://wahb-console.vercel.app
